{
  "name": "functions",
  "description": "Cloud Functions — ALUSO DISTRIBUIDORA (gestión de usuarios)",
  "main": "lib/index.js",
  "private": true,
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "axios": "^1.17.0",
    "fast-xml-parser": "^5.8.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "node-forge": "^1.4.0",
    "soap": "^1.9.3"
  },
  "devDependencies": {
    "@types/node-forge": "^1.3.14",
    "typescript": "^5.5.0"
  }
}

